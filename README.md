# PFE Backend

This is the backend for the PFE project, built with NestJS.

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/deciiim/pfe_backend-.git

# Install dependencies:
npm install

# Running the Application

``To start the development server:
npm run start

``To run the application in production mode:
npm run start:prod


Feel free to adjust this based on what your project does.

### 2. **Save the Changes**
Once you've finished editing, save the changes.

### 3. **Commit the Changes**
Once you've saved your changes to the `README.md` file, commit them to Git:

```bash
git add README.md
git commit -m "Updated README with project details"
